version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # Un solo PR para minors/patches; los majors llegan separados.
      minor-y-patch:
        update-types: ["minor", "patch"]
